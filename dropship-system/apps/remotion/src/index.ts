/**
 * Point d'entrée Remotion — enregistre la racine de l'app vidéo.
 * Lu par : `remotion studio` et `remotion render`.
 */
import { registerRoot } from "remotion";
import { RemotionRoot } from "./Root";

registerRoot(RemotionRoot);
