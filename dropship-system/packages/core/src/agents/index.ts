/**
 * Implémentations TypeScript des 8 agents.
 * Complété à l'étape 5/6 — les fichiers `.claude/agents/*.md` (étape 3) définissent
 * les prompts que Claude Code utilise ; ces implémentations TS orchestrent les
 * appels d'API quand un agent est déclenché en mode automatique via les scripts.
 */
export {};
