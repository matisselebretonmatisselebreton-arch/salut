/**
 * Clients API externes. Les implémentations concrètes arrivent à l'étape 5.
 * Chaque service exporte un factory qui lit les env vars requises et renvoie
 * un client pré-configuré (avec retry/backoff pour les APIs réseau).
 */
export {};
