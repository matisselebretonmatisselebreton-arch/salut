/**
 * Setup interactif.
 * Guide l'utilisateur dans la récupération de chaque clé API puis remplit .env.
 * Implémentation complète à l'étape 6.
 */
console.log(`
┌───────────────────────────────────────────────────────────────┐
│  DROPSHIP SYSTEM — Setup interactif                            │
│  Cet assistant guide la configuration de toutes les APIs.      │
└───────────────────────────────────────────────────────────────┘

⚠️  Non encore implémenté (étape 6 du plan de livraison).

En attendant :
  1. Copier .env.example → .env
  2. Lire README.md §§ "Récupération des clés API"
  3. Lancer :  pnpm run validate-env

`);
