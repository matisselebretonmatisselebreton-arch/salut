/**
 * @dropship/db — façade typée pour Supabase.
 * Les types TS concrets sont générés à l'étape 2 via :
 *   supabase gen types typescript --project-id xxx > src/types.ts
 * et le schéma SQL est dans `schema.sql` à la racine du package.
 */
export {};
